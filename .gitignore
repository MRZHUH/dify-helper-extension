.DS_Store
Thumbs.db

# Packaged builds — zip artifacts aren't tracked, but the image assets
# under dist/ (screenshots, store-screenshots) are.
*.zip

# Editor / OS noise
.idea/
.vscode/
*.swp
*.swo

# Local env
.env
.env.*
